[caps]
    platformName = "Android"
    deviceName = "Oreo"
    app = "features/support/app/app-debug.apk"
    # Use automationName with UiAutomator2, because default automator,
    # not recognize any elements on the screen
    automationName = "UiAutomator2"